-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
ruff==0.8.6
mypy==1.14.1

# ── Reranker research scripts (scripts/test_*reranker*.py, see Documentation/EVALUATION.md 4k) ──
# Not needed to run the core pipeline or API -- only to reproduce those experiments.
torch==2.8.0
setfit==1.1.3
datasets==4.5.0
