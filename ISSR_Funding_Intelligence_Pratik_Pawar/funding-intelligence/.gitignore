# Environment
.env
.envrc

# Local Claude Code guidance (not for GitHub)
claude.md

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/

# Data (generated at runtime)
data/raw/*.jsonl
data/normalised/*.jsonl
data/normalised/*.csv
data/queues/*.db
data/queues/*.db-wal
data/queues/*.db-shm
data/db/*.db
data/db/*.db-wal
data/db/*.db-shm
data/app.db
data/pdfs/
data/embeddings/*.npy
data/embeddings/*.json
data/embeddings/*.faiss
data/evaluation/eval_results.json

# Testing
.pytest_cache/
htmlcov/
.coverage

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker
.dockerignore

# Model caches (large files)
models/
*.bin
*.safetensors

# Harvested NSF award corpus (~4MB, regenerable in one command).
# Kept out of the repo to avoid tripling its size; the benchmark *results*
# derived from it (discipline_benchmark.json) are committed instead.
# Regenerate with: make harvest-nsf-awards
data/evaluation/nsf_awards.jsonl

# SetFit/HF Trainer checkpoint dumps from experiment scripts (scripts/test_setfit_reranker.py) --
# regenerable, hundreds of MB of model weights, never meant to be committed.
checkpoints/
