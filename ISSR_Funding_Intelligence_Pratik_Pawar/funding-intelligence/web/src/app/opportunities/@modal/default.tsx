// Required for the @modal parallel route slot: renders when no intercepted
// route is active (i.e. almost always -- only (.)[foa_id] below fills it).
export default function DefaultModalSlot() {
  return null;
}
